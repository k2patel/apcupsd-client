apiVersion: v1
kind: Service
metadata:
  name: apcupsd-client
  namespace: apcupsd
  labels:
    app.kubernetes.io/name: apcupsd-client
    app.kubernetes.io/part-of: apcupsd-client
spec:
  type: LoadBalancer
  selector:
    app.kubernetes.io/name: apcupsd-client
  ports:
    - name: http
      protocol: TCP
      port: 10280
      targetPort: 8000
