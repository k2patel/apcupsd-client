apiVersion: v1
kind: Service
metadata:
  name: redis
  namespace: apcupsd
  labels:
    app.kubernetes.io/name: redis
    app.kubernetes.io/part-of: apcupsd-client
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: redis
  ports:
    - name: redis
      protocol: TCP
      port: 6379
      targetPort: 6379
